{
  "name": "pairit",
  "version": "0.1.0",
  "description": "",
  "type": "module",
  "private": true,
  "workspaces": [
    "apps/lab/*",
    "apps/manager/*",
    "packages/*"
  ],
  "scripts": {
    "test": "bun test packages/ && bun run --filter @pairit/lab-app test",
    "build": "bun run --filter '*' build",
    "dev": "bun run --filter '@pairit/lab-*' --filter '@pairit/manager-server' dev",
    "emulators": "echo 'Deprecated: use docker compose up'"
  },
  "devDependencies": {
    "@biomejs/biome": "^2.3.14",
    "@types/node": "^24.7.0",
    "typescript": "^5.9.3"
  }
}