{
  "name": "pairit",
  "version": "0.1.0",
  "description": "",
  "type": "module",
  "private": true,
  "workspaces": [
    "manager/*",
    "lab/*",
    "lib/*"
  ],
  "scripts": {
    "test": "bun test",
    "build": "bun run --filter '*' build",
    "dev": "bun run --filter 'lab-*' --filter 'manager-server' dev",
    "emulators": "echo 'Deprecated: use docker compose up'"
  },
  "devDependencies": {
    "@types/node": "^24.7.0",
    "typescript": "^5.9.3"
  },
  "dependencies": {
    "@google-cloud/storage": "^7.14.0",
    "better-auth": "^1.0.7",
    "mongodb": "^6.12.0"
  }
}