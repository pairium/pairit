# Workspace

Embed a collaborative, real-time workspace component alongside chat or survey pages.

Example

```yaml
pages:
  - id: workspace
    components:
      - type: live-workspace
        props:
          documentId: "doc_123"
```


