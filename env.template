# Environment Variables Template
# Copy to .env and fill in values
#
# Local dev:  defaults below work as-is (with local MongoDB + dummy OAuth)
# Production: set real values and the deploy script auto-computes
#              AUTH_BASE_URL / AUTH_TRUSTED_ORIGINS from Cloud Run URLs

# GCP Project (set to your project ID for cloud deployment)
PROJECT_ID="local"

# Database
# Local: mongodb://localhost:27017/pairit
# Production: MongoDB Atlas connection string
MONGODB_URI="mongodb://localhost:27017/pairit"

# Storage
# Local: local filesystem   Production: gcs
STORAGE_BACKEND=local
# Local: path on disk        Production: GCS bucket name
STORAGE_PATH=.storage

# Google OAuth Credentials
GOOGLE_CLIENT_ID="local-dummy-id"
GOOGLE_CLIENT_SECRET="local-dummy-secret"

# Better Auth Secret (generate with: openssl rand -hex 32)
# Has insecure default in dev, required in production
AUTH_SECRET="local-development-secret-must-be-32-chars-long"

# OpenAI API Key (for LLM features)
OPENAI_API_KEY="your-openai-api-key"

# CLI Configuration (defaults to localhost:3002 for local dev)
PAIRIT_API_URL="http://localhost:3002"

# For test scripts and manager server homepage
PAIRIT_LAB_URL="http://localhost:3001"
