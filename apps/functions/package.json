{
  "name": "functions",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "main": "dist/index.js",
  "scripts": {
    "build": "tsup src/index.ts --format esm --dts --out-dir dist",
    "dev": "tsup src/index.ts --format esm --watch --dts --out-dir dist"
  },
  "engines": {
    "node": "22"
  },
  "dependencies": {
    "firebase-admin": "^12.6.0",
    "firebase-functions": "^5.0.1",
    "hono": "^4.5.11"
  },
  "devDependencies": {
    "tsup": "^8.1.0",
    "typescript": "^5.6.2"
  }
}
